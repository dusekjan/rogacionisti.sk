# rogacionisti.sk

## About
This project uses Vite to bundle CSS and TypeScript files for optimized production builds.
No JS/CSS frameworks are used in this project.

## Development
- `npm install`
- `npm run dev`

## Build
- `npm run build` - builds the project for production, output in the `dist` folder



